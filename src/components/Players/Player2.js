import React from 'react'

function Player2(props) {

    return (
        <div className="rectangle-orange"
            style={{
                left: `${props.left}px`,
                top: `${props.top}px`
            }}>

        </div>

    )
}
export default Player2